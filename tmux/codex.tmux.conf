set -g mouse on
set -g status on
set -g status-position bottom
set -g status-style bg=green,fg=black
set -g status-left '[#{session_name}] '
set -g status-right '#(~/.tmux/codex-usage-status.sh "#{session_name}")'
set -g status-interval 5
